"""Trunk III WILL — drives / goals / decision (research/154).

The system's VOLITION: when objectives conflict (return vs risk vs confidence), ARBITRATE them
(`multi_objective_arbitration`) and SCHEDULE which mechanisms/goals to pursue first
(`goal_priority_scheduler`). The arbitration consumes Trunk XIV AXIOLOGY's explicit utility as one of
its objectives — values drive volition. PURE organs; the data (per-mechanism realized returns) comes
from the experience memory.
"""
