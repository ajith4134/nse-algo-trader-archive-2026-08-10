"""Trunk XIV AXIOLOGY — the system's explicit VALUES & practical wisdom (research/153).

The bot has always optimised an IMPLICIT objective (risk/return, buried in `risk_management`). This
trunk makes the objective EXPLICIT and inspectable — a named scalar utility over realized outcomes
(`explicit_utility_function`) — and watches whether realized behaviour DRIFTS from those stated values
(`value_drift_monitor`). PURE organs; the data (real realized returns) comes from the experience memory.
"""
